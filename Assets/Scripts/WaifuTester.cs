using UnityEngine;

public class WaifuTester : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Belle soo cute!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
